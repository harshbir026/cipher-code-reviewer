# pyproject.toml
[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Line too long — Ruff format handles this

[tool.ruff.lint.per-file-ignores]
"app.py" = ["E402"]  # Module level imports not at top (Streamlit pattern)